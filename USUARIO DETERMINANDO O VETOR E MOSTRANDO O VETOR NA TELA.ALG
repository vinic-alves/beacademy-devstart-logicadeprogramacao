Algoritmo "Usuário determinando o Vetor e mostrando o Vetor na tela"

Var
       //Fazendo o usuário determinar o Vetor

   n: vetor[1..6] de inteiro
   i: inteiro

Inicio

para i <- 1 ate 6 faca
     ESCREVA("Digite o " , i, "o valor: ")
     LEIA (n[i])
fimpara

Para i <- 1 ate 6 faca
     Escreva ( "{", n[i], "}" )
fimPara


Fimalgoritmo